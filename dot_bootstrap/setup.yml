# TODO:Add Discord
# BECOME password is my sudo password
---
- name: Machine setup
  hosts: localhost
  become: true
  connection: local
  gather_facts: true

  tasks:
    - name: Install packages
      ansible.builtin.package:
        name:
          - pgcli
          - skopeo
          - bash-language-server
          - pass
        state: present

