# If not running interactively, don't do anything (leave this at the top of this file)
# Skip interactive check during install scripts
if [[ -n "${CHEZMOI_INSTALL_RUNNING:-}" ]]; then
  echo "Skipping interactive check during install..."
elif [[ $- != *i* ]]; then
  return
fi

# All the default Omarchy aliases and functions
# (don't mess with these directly, just overwrite them here!)
source ~/.local/share/omarchy/default/bash/rc
source "$HOME/.config/bash/aliases"
source "$HOME/.config/bash/functions"

export PATH="${KREW_ROOT:-$HOME/.krew}/bin:$PATH"
export PATH="$HOME/.local/share/gem/ruby/3.4.0/bin:$PATH"


. "$HOME/.local/share/../bin/env"

. "$HOME/.atuin/bin/env"

[[ -f ~/.bash-preexec.sh ]] && source ~/.bash-preexec.sh
eval "$(atuin init bash --disable-up-arrow)"


export GRANTED_ALIAS_CONFIGURED="true"

# export TF_VAR_digital_ocean_token=$(pass show ai-agents-do-token)
# export TF_VAR_ssh_private_key_b64=$(pass show argocd-gh-ssh-private-key-encoded)


export PINCLAW_API_URL="https://pinclaw.devpinata.cloud/ops/api/v0"
# export PINCLAW_API_AUTH=$(pass show pinclaw-api-key)
